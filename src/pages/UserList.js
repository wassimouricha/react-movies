import React from 'react';
import Header from '../components/Header';

const UserList = () => {
    return (
        <div>
           <Header></Header>
        </div>
    );
};

export default UserList;